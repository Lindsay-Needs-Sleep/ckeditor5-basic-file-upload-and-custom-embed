import ShowPopup from './popup-form.js';

export default async function GetContentToInsert () {
    // Display the popup form to gether user input
    ShowPopup();

    // Parse the form data and build the content to insert
}
